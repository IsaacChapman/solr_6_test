tests:
  - git branch
test_pattern:
  - none
notify:
  - channel: email
    branch: /^(?!epic-.*$)(?!master$)(?!production$)/
    recipients: 'committers'
    trigger_on: done
    message: "branch did not match"
  - channel: email
    branch: /^(?epic-.*$)(?master$)(?production$)/
    recipients: 'committers'
    trigger_on: done
    message: "branch matched"
